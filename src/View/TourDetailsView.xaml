<UserControl x:Class="TourPlanner.View.TourDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="MediumOrchid"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style x:Key="Value" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="15,3,0,6"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Auto">
        <StackPanel Margin="5">

            <TextBlock Text="{Binding SelectedTour.TourName}" Foreground="DarkOrchid" FontSize="18" FontWeight="Bold" Margin="5"/>

            <TextBlock Text="Description:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.TourDescription}" Style="{StaticResource Value}"/>

            <TextBlock Text="Transport:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.Transport}" Style="{StaticResource Value}"/>

            <TextBlock Text="Starting Point:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.TourStart}" Style="{StaticResource Value}"/>

            <TextBlock Text="End Point:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.TourEnd}" Style="{StaticResource Value}"/>

            <TextBlock Text="Distance:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.Distance, StringFormat='{}{0:F1} km', TargetNullValue='—'}" Style="{StaticResource Value}"/>

            <TextBlock Text="Estimated Time:" Style="{StaticResource Label}"/>
            <TextBlock Text="{Binding SelectedTour.EstTime, StringFormat='{}{0:F0} min', TargetNullValue='—'}" Style="{StaticResource Value}"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
