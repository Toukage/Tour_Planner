<Window x:Class="TourPlanner.View.ModifyTour"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.View"
        mc:Ignorable="d"
        Title="Modify Tour"
        Height="130" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Tour Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tour Name:" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Tour.TourName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <Button Grid.Row="2" Grid.Column="1" Content="Modify Tour" Width="100" Margin="10" Command="{Binding ModifyTourCommand}"/>
    </Grid>
</Window>