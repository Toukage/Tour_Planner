<UserControl x:Class="TourPlanner.View.LogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TourPlanner.View"
             xmlns:conv="clr-namespace:TourPlanner.Converters">
    <UserControl.Resources>
        <conv:NullToBoolConverter x:Key="NullToBool"/>
    </UserControl.Resources>

    <Grid Margin="10,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,6">
            <TextBlock Text="Tour Logs" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <local:CreateBtn  Margin="0,0,0,0" Command="{Binding CreateLogCommand}" IsButtonEnabled="{Binding HasTour}"/>
                <local:DeleteBtn  Margin="0,0,0,0" Command="{Binding DeleteLogCommand}" IsButtonEnabled="{Binding Selected, Converter={StaticResource NullToBool}}"/>
                <local:ModifyBtn  Margin="0,0,0,0" Command="{Binding ModifyLogCommand}" IsButtonEnabled="{Binding Selected, Converter={StaticResource NullToBool}}"/>
            </StackPanel>
        </DockPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Logs}" SelectedItem="{Binding Selected, Mode=TwoWay}" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridViewColumn Header="Date/Time" Width="130"  DisplayMemberBinding="{Binding LogDate, StringFormat={}{0:g}}"/>
                    <GridViewColumn Header="Rating" Width="60"  DisplayMemberBinding="{Binding Rating}"/>
                    <GridViewColumn Header="Difficulty" Width="90"  DisplayMemberBinding="{Binding LogDifficulty}"/>
                    <GridViewColumn Header="Distance" Width="80"  DisplayMemberBinding="{Binding LogDistance}"/>
                    <GridViewColumn Header="Time" Width="70"  DisplayMemberBinding="{Binding LogTime}"/>
                    <GridViewColumn Header="Comment" Width="220"  DisplayMemberBinding="{Binding LogComment}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
